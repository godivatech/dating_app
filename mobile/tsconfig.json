{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    // Path aliases for clean imports — add more as the project grows
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.d.ts",
    "expo-env.d.ts"
  ]
}
