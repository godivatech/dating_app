# shared/

> **Status**: Placeholder — not yet implemented.

This directory will contain **shared TypeScript types, utilities, and constants**
that need to be consumed by both `mobile/` and `backend/` (and potentially `admin/`).

## Guiding principle

Nothing goes here until there is an actual, concrete need for it.
Shared code that only lives in one package stays in that package.

## Potential future contents

- Shared Zod schemas (API contracts)
- Shared TypeScript types (DTOs, enums)
- Shared utility functions (date formatting, validation helpers)

No code exists here yet.
